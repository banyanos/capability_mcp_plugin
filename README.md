# capability_mcp_plugin

Banyanos capability-facing MCP compatibility plugin.

This project should depend on `silvaengine_daemon` for shared daemon runtime
behavior and delegate native MCP catalog/runtime work to `mcp_protocol_plugin`.

See [Capability MCP Plugin Development Plan](docs/DEVELOPMENT_PLAN.md).
